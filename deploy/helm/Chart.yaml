# Reference: https://helm.sh/docs/topics/charts/

# Chart / app (sync with Cargo.toml)
name: api-snap
version: 0.1.0
appVersion: "0.1.0"
description: An application which collects API documents by kubernetes service discovery and displays them in a web interface.
keywords:
    - kubernetes
    - documentation
    - openapi
    - asyncapi
home: https://youka.dev/api-snap
sources:
    - https://github.com/Youka/api-snap
icon: https://youka.dev/api-snap/favicon.png

# Chart exclusive
apiVersion: v2
type: application

# Kubernetes
kubeVersion: ">=1.24.0"
