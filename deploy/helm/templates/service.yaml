apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-snap.fullname" . }}
  labels:
    {{- include "api-snap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-snap.selectorLabels" . | nindent 4 }}
