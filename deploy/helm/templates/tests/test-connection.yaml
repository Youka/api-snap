apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-snap.fullname" . }}-test-connection"
  labels:
    {{- include "api-snap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-snap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
